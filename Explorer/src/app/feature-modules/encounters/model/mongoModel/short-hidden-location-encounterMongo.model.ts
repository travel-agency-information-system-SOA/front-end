export interface ShortHiddenLocationEncounterMongo {
    id: string,
    encounterId: string,
    imageURL: string,
    imageLatitude: number,
    imageLongitude: number,
    distanceTreshold: number
}